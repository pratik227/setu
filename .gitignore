node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.local
apps/desktop/src-tauri/target/
# Regenerated by tauri-build on every compile from tauri.conf.json and
# capabilities/. Committing it means reviewing a machine-written diff on every
# Tauri bump, and a stale copy is worse than none: the editor validates the
# capability file against a schema the runtime no longer uses.
apps/desktop/src-tauri/gen/
test-results/
playwright-report/
